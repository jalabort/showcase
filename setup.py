from setuptools import setup, find_packages


setup(
    name="showcase",
    version="0.0.0",
    author="hudlrd",
    install_requires=[
        'hudl-terrarium',
    ],
    packages=find_packages()
)
